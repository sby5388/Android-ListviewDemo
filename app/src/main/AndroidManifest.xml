<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.by5388.demo.listview">

    <application
        android:name=".ListViewApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".choice.mode.ListViewNoneActivity"
            android:label="@string/model_default" />
        <activity
            android:name=".choice.mode.ListViewSingleActivity"
            android:label="@string/model_single" />
        <activity
            android:name=".choice.mode.ListViewMultipleActivity"
            android:label="@string/model_multiple" />
        <activity
            android:name=".choice.mode.ListViewMultipleWithActionBarActivity"
            android:label="@string/model_multiple_with_actionbar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>